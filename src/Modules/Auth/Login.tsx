import {View} from 'react-native';
import AppText from '../../Components/AppText';

export default function Login() {
  return (
    <View style={{flex: 1}}>
      <AppText>This new login Screen</AppText>
    </View>
  );
}
